    file main: main.c   # main routine
    file make: makefile
    # entry 'buff' does not belong in lexicon. Therefore not included.
    buff .gitignore

  -
  loop string file --ui window misc
  tab $_:src,file $_.c,file $_.h
  endl

  loop logic -math physics
  tab $_:src,file $_.c,file $_.h # single line entries
  endl

workspace meta {
    file read:README.md
    file clog:changelog
  tab bin: bin
    file install.sh
   -file uninstall.sh
    file deploy.sh
}
workspace :doc { Workspace skips tabs. Goes directly to leaf nodes
  file user: user.txt
  file dev : dev.txt
  file api : api.txt
}

#leafless tree dies!
workspace,tab


