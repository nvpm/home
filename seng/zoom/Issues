
|bugs|
  ● [bug] au WinResized removes visual & possibly other things ~
  ● [bug] leaving a term window upon move key press ~
    ● upon leaving term window after terminal is exited, 
      using any method (<c-d>, exit command, etc), and pressing, 
      and holding h,j,k, and l. The whole zoomed buffer goes left,
      butit keeps its size.
|fixed|
  ● still some help files are resizing buf when loaded ~
    ● still needs some pay attention to
  ● double help pagination w/ 'K' ~
    ● the second call for the help file triggers the 'helpclose'
      instruction from the autocmd

vim: ft=help: tw=2
