
  ● skel feature ~
  ● var-replace ~
  ● flux#cuts takes higher standalone cut instead of next node's ~
  ● use "=" as absolute path instead of "@", and then remove @ support ~
  ● synx ~
   ● create syntax for single line multiple statement (endline feature)
     file a,file b,...file n

vim: ft=help: tw=2
