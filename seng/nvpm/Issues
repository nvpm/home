|bugs|
  ● [bug] files not expanding or resolving ~
  ● [bug] ~
  ● [bug] ~

|fixed|
  ● [bug] orphaned terminal buffers after edit mode reparsing ~
  Terminals owned by a leaf node get left behind after arbo#arbo() call.
  ● [bug] savedtree on last terminal visited, does not recreate term ~
  ● on NvpmTerm close erases undo tree in Neovim ~
    Solution was to re-use the :terminal command. The use of jobstart() 
    deletes the current buffer, which then erases the its undo-tree
  ●  ~

vim: ft=help: tw=2
